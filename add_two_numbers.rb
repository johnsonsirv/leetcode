# Definition for singly-linked list.
class ListNode
    attr_accessor :val, :next
    def initialize(val)
        @val = val
        @next = nil
    end

    def reverse
      
    end
end

# @param {ListNode} l1
# @param {ListNode} l2
# @return {ListNode}
def add_two_numbers(l1, l2)
    #travers the list and grab nodes in reverse order, return reversed
    # store in a list and perform addition
    # for each digit in sum <list> reverse
    # return linkedlsit
    node = ListNode.new()
    l1_reversed = 
end